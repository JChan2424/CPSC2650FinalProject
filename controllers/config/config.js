const config = {}
config.SECRET = 'hentai' // The secret key used to sign JWT tokens
config.INVITE = 'bruh' // The invite code to create an account with elevated permissions
config.SESSION_LENGTH = '6h' // How long a login session lasts

module.exports = config