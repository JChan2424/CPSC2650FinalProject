const config = {}
config.SECRET = 'boku no piku' // The secret key used to sign JWT tokens
config.INVITE = 'CPSC2650' // The invite code to create an account with elevated permissions
config.SESSION_LENGTH = '6h' // How long a login session lasts

module.exports = config